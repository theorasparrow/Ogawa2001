-- import Ogawa2001.SmallErrorOfCleverTest


/- (7) exists clever test -/
-- lemma direct_part_rw :
--     ∃ Aₙ,
--       (
--         lim at_top, αₙ(Aₙ) = 0
--         ∧
--         ( limsup at_top, 1/n * log βₙ(Aₙ) ) ≤ -D(ρ||σ)
--       ) := by
--   -- use Sₙ
--   -- split
--   -- exact [error_αₙ_of_test_Sₙ_for_large_n]
--   -- exact [error_βₙ_of_test_Sₙ_for_large_n]
--   sorry
